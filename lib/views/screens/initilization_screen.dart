import 'package:flutter/material.dart';

class InitilizationScreen extends StatelessWidget {
  const InitilizationScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
